<div class="header" role="banner">
    <div class="header-backdrop">
        <div class="header-container">
            {% if pages["_meta"].meta.logo %}
                <a class="logo" href="{{ base_url }}">
                    <img src="{{ base_url }}/{{ pages["_meta"].meta.logo|url }}" alt="" />
                </a>
            {% endif %}
            {# ! Tagline is not needed for my design, keeping it to have something to refer to later #}
            {# <div class="title" {{ pages["_meta"].meta.tagline ? ' class="tagline"' }}>
                <a href="{{ base_url }}">
                    <h1>{{ site_title }}</h1>
                    {{ pages["_meta"].meta.tagline|markdown }}
                </a>
            </div> #}

            {# ! Same here, I would like to solve the burger menu without the usage of javascript #}
            {# <a class="nav-toggle" id="nav-toggle" title="Toggle Menu" role="button" aria-controls="nav"
                aria-expanded="false" tabindex="1">
                <i class="fa fa-bars" aria-hidden="true" id="toggler"></i>
                <span class="sr-only">Toggle Menu</span>
            </a> #}

                <div class="nav" id="nav" role="navigation" tabindex="-1">
                    <ul>
                        {% for page in pages(depthOffset=-1) if page.title and not page.hidden %}
                        <li{% if page.id==current_page.id %} class="active" {% endif %}>
                            <a href="{{ page.url }}">
                                {% if page.meta['logo'] %}<i class="{{ page.meta['logo'] }}" aria-hidden="true"></i>{% endif %}
                                {{ page.title }}
                            </a>
                        </li>
                        {% endfor %}
                    </ul>
                </div>

        </div>
    </div>
</div>